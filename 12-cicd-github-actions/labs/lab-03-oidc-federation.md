# Lab 03: GitHub Actions OIDC for AWS
## Steps
1. Create OIDC provider in AWS IAM for GitHub
2. Create IAM role with GitHub trust policy
3. Configure workflow to use `aws-actions/configure-aws-credentials`
4. Test deployment without any static credentials
5. Scope permissions to specific repo/branch
